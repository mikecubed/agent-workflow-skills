{
  "name": "agent-orchestration",
  "version": "3.0.1",
  "description": "Umbrella marketplace for workflow, clean-code enforcement, and design pattern plugins.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "node --test test/**/*.test.js && npm --prefix plugins/flow test && npm --prefix plugins/ccc test && npm --prefix plugins/patterns test",
    "validate:plugin": "npm test",
    "validate:runtime": "node scripts/verify-runtime.mjs"
  },
  "files": [
    ".github/plugin/",
    ".claude-plugin/",
    "docs/",
    "plugins/",
    "README.md",
    "LICENSE",
    "CONTRIBUTING.md"
  ],
  "engines": {
    "node": ">=22.0.0"
  }
}
